provider: openrouter
model: google/gemini-3.6-flash
fallback_models:
  - google/gemini-3.5-flash
  - openai/gpt-4o-mini

review:
  max_iterations: 3
  severity_threshold: medium

paths:
  ignore:
    - node_modules
    - dist
    - build
    - coverage
    - .git
    - .pushfox
